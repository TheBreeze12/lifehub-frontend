# LifeHub 快速开始指南

## 📱 5分钟快速体验

### 步骤1：启动后端服务 (2分钟)

```bash
# 1. 打开终端，进入后端目录
# 2. 激活虚拟环境
venv\Scripts\activate
# 3. 确保已配置.env文件（包含DASHSCOPE_API_KEY和数据库配置）
# 4. 启动服务
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**验证后端是否启动成功：**
- 浏览器访问：http://localhost:8000/health
- 应该看到：`{"status":"ok","api_key_configured":true}`
- 访问：http://localhost:8000/docs 查看交互式API文档
- 如果 `api_key_configured` 为 `false`，检查 `.env` 文件中的 `DASHSCOPE_API_KEY` 配置

### 步骤2：运行Android应用 (3分钟)

1. **打开项目**
   - 启动Android Studio
   - 打开LifeHub项目

2. **等待Gradle同步**
   - 首次打开会自动同步依赖
   - 等待底部进度条完成

3. **运行应用**
   - 点击顶部工具栏的绿色▶️按钮
   - 选择模拟器或连接的真机
   - 等待应用安装并启动

### 步骤3：测试功能 (1分钟)

#### 测试用户注册和登录
1. 应用启动后，进入"我的"页面
2. 点击"登录"按钮
3. 如果没有账号，点击"没有账号？注册一个"
4. 填写昵称和密码完成注册
5. 使用注册的账号登录

#### 测试菜品营养查询
1. 进入"首页"或"餐饮"页面
2. 在输入框中输入：`番茄炒蛋`
3. 点击"查询"按钮
4. 等待几秒，查看AI返回的营养分析结果

#### 测试菜单识别
1. 进入"餐饮"页面
2. 点击拍照按钮或从相册选择菜单图片
3. 等待AI识别（通常5-10秒）
4. 查看识别出的菜品列表和营养分析

#### 测试运动规划
1. 进入"运动"页面
2. 输入运动需求（如："规划餐后运动，消耗300卡路里"）
3. 点击"生成计划"按钮
4. 查看生成的运动计划详情

## 🎯 第一次运行检查清单

### ✅ 后端检查

- [ ] Python 3.10+ 已安装
- [ ] 虚拟环境已创建并激活
- [ ] 依赖已安装 (`pip install -r requirements_simple.txt`)
- [ ] .env文件已创建，包含有效的`DASHSCOPE_API_KEY`
- [ ] 数据库已创建并配置（MySQL）
- [ ] 数据库表已初始化（运行 `python utils/init_database.py`）
- [ ] 后端服务已启动，监听8000端口
- [ ] 访问 http://localhost:8000/docs 能看到API文档
- [ ] 访问 http://localhost:8000/health 返回正常状态

### ✅ Android检查

- [ ] Android Studio已安装（Koala | 2024.1.1+）
- [ ] JDK 11+已配置
- [ ] 项目Gradle同步成功
- [ ] 模拟器已创建或真机已连接
- [ ] AndroidManifest.xml包含网络权限和相机权限
- [ ] AndroidManifest.xml设置了 `usesCleartextTraffic="true"`（允许HTTP）
- [ ] 应用能成功构建和运行
- [ ] RetrofitClient.kt中的BASE_URL配置正确（模拟器：10.0.2.2，真机：电脑IP）
- [ ] 已测试用户注册和登录功能

## 🔧 常见问题快速解决

### 问题1：后端启动失败

**错误：`DASHSCOPE_API_KEY environment variable not set`**

**解决方案：**
```bash
# 在backend目录下创建.env文件
# 内容如下：
DASHSCOPE_API_KEY=sk-your_api_key_here
HOST=0.0.0.0
PORT=8000
```

### 问题2：Android连接后端失败

**错误信息：** "网络连接失败，请检查后端服务是否启动"

**解决方案：**

**使用模拟器：**
- 确认代码中使用的是 `http://10.0.2.2:8000` （已配置）
- 检查后端是否在运行

**使用真机：**
```kotlin
// 修改 RetrofitClient.kt
private const val BASE_URL = "http://你的电脑IP:8000"

// 查看电脑IP：
// Windows: ipconfig
// Linux/Mac: ifconfig
```

### 问题3：Gradle同步失败

**解决方案：**
```bash
# 方法1：清理并重新构建
Build → Clean Project
Build → Rebuild Project

# 方法2：使Gradle缓存失效
File → Invalidate Caches → Invalidate and Restart

# 方法3：检查网络连接，重新同步
File → Sync Project with Gradle Files
```

### 问题4：AI返回"无法解析"错误

**原因：** AI返回的JSON格式不标准

**解决方案：**
- 这是正常现象，AI偶尔会返回格式错误
- 代码中已经处理了这种情况
- 重新查询即可
- 如果持续出现，检查后端日志

### 问题5：用户注册失败

**错误信息：** "用户已存在" 或 "网络请求失败"

**解决方案：**
- 检查后端服务是否正常运行
- 确认网络连接正常
- 尝试使用不同的昵称
- 检查后端数据库连接是否正常

### 问题6：登录失败

**错误信息：** "用户不存在" 或 "密码错误"

**解决方案：**
- 确认已注册账号
- 检查昵称和密码是否正确
- 确认后端服务正常运行
- 查看后端日志了解详细错误
