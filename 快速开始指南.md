# LifeHub 快速开始指南

## 📱 5分钟快速体验

### 步骤1：启动后端服务 (2分钟)

```bash
# 1. 打开终端，进入后端目录
cd C:\Users\86166\OneDrive\桌面\软件创新\backend

# 2. 激活虚拟环境
venv\Scripts\activate

# 3. 确保已配置.env文件（包含DASHSCOPE_API_KEY）

# 4. 启动服务
uvicorn app.main:app --reload
```

**验证后端是否启动成功：**
- 浏览器访问：http://localhost:8000/health
- 应该看到：`{"status":"ok","message":"Backend is running"}`

### 步骤2：运行Android应用 (3分钟)

1. **打开项目**
   - 启动Android Studio
   - 打开项目：`C:\Users\86166\OneDrive\桌面\软件创新\LifeHub`

2. **等待Gradle同步**
   - 首次打开会自动同步依赖
   - 等待底部进度条完成

3. **运行应用**
   - 点击顶部工具栏的绿色▶️按钮
   - 选择模拟器或连接的真机
   - 等待应用安装并启动

### 步骤3：测试功能 (1分钟)

1. 应用启动后，你会看到"智能餐饮"页面
2. 在输入框中输入：`番茄炒蛋`
3. 点击"查询"按钮
4. 等待几秒，查看AI返回的营养分析结果

## 🎯 第一次运行检查清单

### ✅ 后端检查

- [ ] Python 3.10+ 已安装
- [ ] 虚拟环境已创建并激活
- [ ] 依赖已安装 (`pip install -r requirements_simple.txt`)
- [ ] .env文件已创建，包含有效的`DASHSCOPE_API_KEY`
- [ ] 后端服务已启动，监听8000端口
- [ ] 访问 http://localhost:8000/docs 能看到API文档

### ✅ Android检查

- [ ] Android Studio已安装
- [ ] JDK 11+已配置
- [ ] 项目Gradle同步成功
- [ ] 模拟器已创建或真机已连接
- [ ] AndroidManifest.xml包含网络权限
- [ ] 应用能成功构建和运行

## 🔧 常见问题快速解决

### 问题1：后端启动失败

**错误：`DASHSCOPE_API_KEY environment variable not set`**

**解决方案：**
```bash
# 在backend目录下创建.env文件
# 内容如下：
DASHSCOPE_API_KEY=sk-your_api_key_here
HOST=0.0.0.0
PORT=8000
```

### 问题2：Android连接后端失败

**错误信息：** "网络连接失败，请检查后端服务是否启动"

**解决方案：**

**使用模拟器：**
- 确认代码中使用的是 `http://10.0.2.2:8000` （已配置）
- 检查后端是否在运行

**使用真机：**
```kotlin
// 修改 RetrofitClient.kt
private const val BASE_URL = "http://你的电脑IP:8000"

// 查看电脑IP：
// Windows: ipconfig
// Linux/Mac: ifconfig
```

### 问题3：Gradle同步失败

**解决方案：**
```bash
# 方法1：清理并重新构建
Build → Clean Project
Build → Rebuild Project

# 方法2：使Gradle缓存失效
File → Invalidate Caches → Invalidate and Restart

# 方法3：检查网络连接，重新同步
File → Sync Project with Gradle Files
```

### 问题4：AI返回"无法解析"错误

**原因：** AI返回的JSON格式不标准

**解决方案：**
- 这是正常现象，AI偶尔会返回格式错误
- 代码中已经处理了这种情况
- 重新查询即可
- 如果持续出现，检查后端日志

## 📸 预期效果截图描述

### 初始状态
- 顶部：森林绿导航栏，显示"智能餐饮"
- 中间：白色卡片，包含输入框和橙色查询按钮
- 底部：空状态卡片，显示🍱图标和提示文字

### 查询中状态
- 查询按钮显示加载动画（圆形进度条）
- 按钮被禁用，无法重复点击

### 成功状态
- 显示白色结果卡片
- 顶部：菜品名称（黑色粗体）
- 中间：4个米色小卡片，分别显示热量、蛋白质、脂肪、碳水
  - 数值用橙色显示
  - 单位用灰色小字
- 底部：AI推荐，带🤖图标，绿色"AI推荐"标题

### 错误状态
- 显示错误卡片
- ❌图标
- 红色错误信息

## 🧪 测试用例

### 测试1：正常查询
```
输入：番茄炒蛋
预期：返回营养成分和推荐理由
```

### 测试2：复杂菜名
```
输入：宫保鸡丁
预期：返回营养成分和推荐理由
```

### 测试3：空输入
```
输入：（留空）
预期：显示"请输入菜品名称"错误
```

### 测试4：未知菜品
```
输入：xyz不存在的菜
预期：AI会估算或返回"无法获取该菜品信息"
```

### 测试5：后端未启动
```
操作：停止后端服务，然后查询
预期：显示"网络连接失败，请检查后端服务是否启动"
```

## 📝 开发调试技巧

### 1. 查看后端日志
```bash
# 后端终端会显示所有API请求
# 包括请求体、AI响应、错误信息
```

### 2. 查看Android日志
```bash
# Android Studio底部Logcat窗口
# 过滤标签：System.out 或搜索 "FoodViewModel"
```

### 3. 测试后端API
```bash
# 使用curl测试
curl -X POST http://localhost:8000/api/food/analyze \
  -H "Content-Type: application/json" \
  -d "{\"food_name\": \"番茄炒蛋\"}"

# 或者运行测试脚本
cd backend
python test_api.py
```

### 4. 使用API文档
```
浏览器访问：http://localhost:8000/docs
可以直接在网页中测试API
```

## 🎓 学习建议

### 如果你是新手

1. **先理解架构**
   - 阅读README.md了解项目结构
   - 查看MainActivity.kt → FoodScreen.kt → FoodViewModel.kt的调用链

2. **学习顺序**
   - 第1天：熟悉Jetpack Compose基础
   - 第2天：理解MVVM架构
   - 第3天：学习Retrofit网络请求
   - 第4天：掌握Kotlin Coroutines和Flow

3. **推荐资源**
   - Jetpack Compose官方教程：https://developer.android.com/jetpack/compose/tutorial
   - Kotlin协程：https://kotlinlang.org/docs/coroutines-overview.html
   - Retrofit使用：https://square.github.io/retrofit/

### 如果你有经验

- 直接查看代码，重点关注：
  - `FoodViewModel.kt`: 状态管理和网络请求逻辑
  - `FoodScreen.kt`: Compose UI组件和状态绑定
  - `RetrofitClient.kt`: 网络配置和错误处理

## 🚀 下一步

完成基础功能后，可以尝试：

1. **添加历史记录**
   - 使用Room数据库保存查询历史
   - 在UI上显示最近查询

2. **添加收藏功能**
   - 允许用户收藏菜品
   - 显示收藏列表

3. **优化UI**
   - 添加动画效果
   - 改进布局和交互

4. **扩展功能**
   - 拍照识别菜品（OCR）
   - 语音输入
   - 智能出行规划

---

**祝你开发顺利！如有问题，请参考README.md或查看代码注释。** 🎉

